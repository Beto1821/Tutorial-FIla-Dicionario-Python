# Dependências do projeto Python - Filas e Dicionários - Versão Otimizada

# Dependências principais (Python stdlib - não precisa instalar)
# collections, queue, heapq, time, typing já inclusos no Python

# Para desenvolvimento e formatação (opcional)
black>=21.0.0
flake8>=3.8.0
mypy>=0.900

# Para testes (opcional)
pytest>=6.0.0
pytest-cov>=2.10.0

# Para performance profiling (opcional)
memory-profiler>=0.58.0
psutil>=5.8.0